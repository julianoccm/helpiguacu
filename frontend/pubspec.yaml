name: frontend
description: "Help Iguaçu App Frontend"
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  smooth_page_indicator: ^1.1.0
  http: ^1.2.0
  bcrypt: ^1.1.3
  shared_preferences: ^2.1.1
  mask_text_input_formatter: ^2.9.0
  flutter_feather_icons: ^2.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/